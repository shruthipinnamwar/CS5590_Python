#!/usr/bin/python
import platform
print(platform.python_version())
print('Print: In Python 2, “print” is treated as a statement rather than a function.'
      'Integer Division: Python 2 treats numbers that you type without any digits after the decimal point as integers, '
      'List Comprehension Loop Variables:'
      'Unicode Strings: Python 3 stores strings as Unicode by default, whereas Python 2 requires you to mark a string with '
      'a “u” if you want to store it as Unicode.'
      'Raising Exceptions: Python 3 requires different syntax for raising exceptions. '
      '')